query ExampleQuery($id: Int) {
    Media(id: $id, type: ANIME){
        id
        title {
            romaji
            english
            native
        }
        coverImage{
            large
        }
    }
}